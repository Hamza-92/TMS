# Feature modules

Feature folders are created for `auth`, `dashboard`, `customers`,
`measurements`, `orders`, `payments`, `subscription`, and `settings`.

Add `data`, `domain`, and `presentation` layers inside a feature only when that
feature is implemented. No business feature is implemented in this foundation.
